# Base runtime — the fallback pipeline runs on these alone (no torch, no GPU).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
numpy>=1.26
# Opt-in WebRTC transport (MT_WEBRTC=1). Wheel-only deps (PyAV, pylibsrtp), so this
# keeps the install portable across CI runners; unused unless the transport is enabled.
aiortc>=1.9
